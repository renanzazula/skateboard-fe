// Web has no native splash screen to hand off from (see the Expo splash
// config, which only targets iOS/Android) — nothing to overlay here.
export function AnimatedSplashOverlay(_props: { ready: boolean }) {
  return null;
}
